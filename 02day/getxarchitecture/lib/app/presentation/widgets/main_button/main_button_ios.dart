import 'package:flutter/cupertino.dart';

class MainButtonIOS extends StatelessWidget {
  const MainButtonIOS({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("Main button iOS");
  }
}